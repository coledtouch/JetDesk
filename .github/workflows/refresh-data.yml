name: Refresh airport data

on:
  schedule:
    - cron: '17 9 * * 4'   # every Thursday 09:17 UTC; NASR cycles publish on Thursdays every 28 days
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: site
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check the current NASR cycle
        id: check
        run: |
          python3 refresh_data.py --check | tee check.txt
          echo "changed=$(grep -c '^changed$' check.txt || true)" >> "$GITHUB_OUTPUT"
      - name: Rebuild the dataset
        if: steps.check.outputs.changed == '1' || github.event_name == 'workflow_dispatch'
        run: python3 refresh_data.py --force
      - name: Deploy to Cloudflare Pages
        if: steps.check.outputs.changed == '1' || github.event_name == 'workflow_dispatch'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        # wrangler.toml in this repo carries <PLACEHOLDER> binding ids; with it present, wrangler tries to apply
        # them to the project and the deploy fails (Sep 3 2026). Deploy without it: the project keeps the
        # bindings and secrets configured in the dashboard, and functions/ is picked up from the working directory.
        run: |
          mv wrangler.toml wrangler.toml.example
          npx --yes wrangler@4 pages deploy dist --project-name meridian-flight-desk --commit-dirty=true --branch main
          mv wrangler.toml.example wrangler.toml
      - name: Commit the refreshed dataset
        if: steps.check.outputs.changed == '1' || github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "jetdesk-bot"
          git config user.email "hello@jetdesk.ai"
          git add airports_us.json dataset_cycle.json app_head.html app.js airports.py legal.py
          git commit -m "Refresh airport data to NASR cycle $(python3 -c "import json;print(json.load(open('dataset_cycle.json'))['cycle'])")" || echo "nothing to commit"
          git push
